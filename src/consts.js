// Offset for scrollToElement to account for nav height
const scrollToOffset = -50;

// Max number of characters to show in a blog post preview. If there 
// are more characters, then they get truncated and replaced with '...'
const maxNumOfCharInBlogPostPrev = 50;

export { scrollToOffset, maxNumOfCharInBlogPostPrev };